<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Console appender with custom pattern -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <!-- Format: 2026-07-09 20:47:42 [thread-name] [TraceID: value] LEVEL ClassName - Message -->
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] [TraceID: %X{traceId}] %-5level %logger{0} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- File appender for logging to a file -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/notification-service.log</file>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] [TraceID: %X{traceId}] %-5level %logger{0} - %msg%n</pattern>
        </encoder>
        <!-- Rolling policy: Create a new file daily -->
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>logs/notification-service.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <timeBasedFileNamingAndTriggeringPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP">
                <maxFileSize>10MB</maxFileSize>
            </timeBasedFileNamingAndTriggeringPolicy>
            <maxHistory>30</maxHistory>
        </rollingPolicy>
    </appender>

    <!-- Root logger configuration -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </root>

    <!-- Application-specific logger configurations -->
    <logger name="com.project.notification_service" level="DEBUG"/>
    <logger name="org.springframework" level="INFO"/>
    <logger name="org.springframework.kafka" level="DEBUG"/>
    <logger name="org.apache.kafka" level="INFO"/>
</configuration>
